[project]
name = "llada-minimal-quickstart"
version = "0.1.0"
description = "LLaDA minimal quickstart fork."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
	"argparse>=1.4.0",
	"torch>=2.6.0",
	"transformers>=4.0",
]
